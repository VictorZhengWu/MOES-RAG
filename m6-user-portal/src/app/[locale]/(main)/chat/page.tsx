/**
 * New chat page — no conversation ID, empty message list.
 */

'use client';

import { ChatPanel } from '@/components/chat/chat-panel';

export default function ChatPage() {
  return <ChatPanel />;
}
