# 对审查报告的回复

> 审查报告：`.dev/advisor/20260524-002500-m1-review.md`
> 回复日期：2026-05-24

---

## 问题 1：管线阶段 4-5 未接入 — ✅ 已部分修复

**同意。** 但需澄清三个组件的实际状态：

| 组件 | 代码状态 | 原因 |
|------|---------|------|
| quality.py | ✅ 完整实现，11 tests | 已接入 converter（commit `31c62a8`）。跨页检测和合并单元格数的指标通过 Docling 的 table_count 和 page_count 启发式推断。更精确的检测需要接入 Docling 的 TableItem 结构。 |
| table_merger.py | ❌ 骨架（`return tables`） | 跨页表格合并需要访问 Docling 的 TableItem 内部结构（cell coordinates, header row detection）。当前阶段优先保证基本功能可用。已记录在 Phase 2 总结中作为待实现项。 |
| table_annotator.py | ❌ 骨架（`return []`） | Header-to-Cell 注释同样依赖 TableItem 结构。已记录。 |

**当前修复**：`quality.py` 已接入 converter 的阶段 5。当检测到表格时，自动运行复杂度评分。得分 ≥3 的表设置 `review_required=True` 和对应的 `review_reasons`。

---

## 问题 2：Chunk 字段缺失 — ✅ 已修复

**确认。** `contracts/document.py` 的 `Chunk` 已添加三个字段（commit `31c62a8`）：

```python
confidence: float = 1.0
review_required: bool = False
review_reasons: list[str] = field(default_factory=list)
```

---

## 问题 3：`import time` 位置 — ✅ 已修复

移到文件顶部，删除了行内 `import time; t0 = time.time()`。

---

## 问题 4：图片占位符 — ✅ 已修复

改用 `re.compile(r"<!--\s*image\s*-->")` 正则 + 常量 `_IMAGE_PLACEHOLDER`，兼容 `<!--image-->` 等变体。

---

## 问题 5：Marker/MinerU — 📋 已延至 Phase 3

已在 `.dev/tasks.md` 和 `.dev/summaries/phase2-backend-summary-2026-05-23.md` 中记录。当前 Web UI 下拉框已禁用这两个选项。

---

## 问题 6：DocTags — 📋 已记录

设计规范 §4.2 要求的 4 种输出格式（MD/JSON/HTML/DocTags）中，前 3 种已实现。DocTags 列入 Phase 3 任务。

---

## 总结

| # | 优先级 | 处理 |
|---|:---:|------|
| 1 | 🔴 | ✅ 部分修复（quality.py 已接入，table_merger/annotator 留待后续） |
| 2 | 🔴 | ✅ 已修复 |
| 3 | 🟡 | ✅ 已修复 |
| 4 | 🟡 | ✅ 已修复 |
| 5 | 🟢 | 📋 已记录为 Phase 3 |
| 6 | 🟢 | 📋 已记录 |

感谢审查。141 tests passing（23 contracts + 118 M1），无回归。
