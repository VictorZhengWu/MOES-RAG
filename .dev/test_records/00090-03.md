# 00090-03 - Prompt 管理器 测试记录

## 第 1 次尝试

**时间**：2026-06-03 22:00:00
**状态**：✅ 通过

### 测试用例 1：test_get_prompt_english
- 输入：set_prompt("system", "en", "System Prompt", "You are an expert assistant.") → get_prompt("system", "en")
- 期望输出："You are an expert assistant."
- 实际输出："You are an expert assistant."
- 结果：✅ 通过

### 测试用例 2：test_get_prompt_chinese_fallback
- 输入：set_prompt("system", "en", "...") → get_prompt("system", "cn")
- 期望输出：英语版本模板（语言降级 en 回退）
- 实际输出："Answer based on context."（英语版本）
- 结果：✅ 通过

### 测试用例 3：test_get_prompt_hardcoded_default
- 输入：get_prompt("system_en", "en")（无任何 DB 数据）
- 期望输出：硬编码默认模板，包含 "Marine & Offshore Engineering" 和 "{retrieved_context}"
- 实际输出：包含上述内容
- 结果：✅ 通过

### 测试用例 4：test_set_prompt_upsert
- 输入：set_prompt("greeting", "en", "Greeting", "Hello") → set_prompt("greeting", "en", "Greeting V2", "Hello, world!") → get_prompt("greeting", "en")
- 期望输出："Hello, world!"（更新的内容）
- 实际输出："Hello, world!"
- 结果：✅ 通过

### 测试用例 5：test_fill_template
- 输入：template = "Context: {retrieved_context}. Graph: {graph_context}."，fill_template(retrieved_context="Doc 1", graph_context="Entity: Ship")
- 期望输出："Context: Doc 1. Graph: Entity: Ship."
- 实际输出："Context: Doc 1. Graph: Entity: Ship."
- 结果：✅ 通过

### 测试用例 6：test_list_prompts
- 输入：set_prompt("system", "en", ...) → set_prompt("citation", "en", ...) → list_prompts()
- 期望输出：2 条记录，prompt_id 为 {"system", "citation"}
- 实际输出：2 条记录，prompt_id 为 {"system", "citation"}
- 结果：✅ 通过

### 总结
- 通过用例数：6 / 6
- 失败原因分析：无
- 修复方向：无
- 备注：全量测试套件 33/33 通过，无回归
