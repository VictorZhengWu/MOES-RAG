# 00060-01 - 项目骨架与配置系统 测试记录

## 第 1 次尝试

**时间**：2026-05-21 23:30:00
**状态**：✅ 通过

### 测试用例 1：GPU >= 8GB + Linux -> 推荐 vLLM + PaddleOCR-VL
- 输入：mock GPU 12GB VRAM + Linux 系统
- 期望输出：recommended_backend="paddleocr_vl", can_use_vllm=True
- 实际输出：recommended_backend="paddleocr_vl", can_use_vllm=True
- 结果：✅ 通过

### 测试用例 2：GPU >= 8GB + Windows -> 推荐 Transformers + GraniteDocling
- 输入：mock GPU 10GB VRAM + Windows 系统
- 期望输出：recommended_backend="granite_docling", can_use_vllm=False
- 实际输出：recommended_backend="granite_docling", can_use_vllm=False
- 结果：✅ 通过

### 测试用例 3：GPU < 8GB -> 推荐 Standard Pipeline
- 输入：mock GPU 6GB VRAM
- 期望输出：recommended_backend="docling_standard"
- 实际输出：recommended_backend="docling_standard"
- 结果：✅ 通过

### 测试用例 4：无 GPU -> 推荐 CPU 模式 + EasyOCR
- 输入：mock torch.cuda.is_available()=False
- 期望输出：gpu=False, vram_gb=0, recommended_ocr="easyocr", can_use_vllm=False
- 实际输出：gpu=False, vram_gb=0, recommended_ocr="easyocr", can_use_vllm=False
- 结果：✅ 通过

### 测试用例 5：OCR 引擎优先级顺序
- 输入：直接读取 OCR_ENGINE_PRIORITY 常量
- 期望输出：paddleocr 第一，tesseract 最后
- 实际输出：paddleocr 第一，tesseract 最后
- 结果：✅ 通过

### 测试用例 6：支持的后端列表
- 输入：直接读取 SUPPORTED_BACKENDS 常量
- 期望输出：包含 docling, marker, mineru
- 实际输出：包含 docling, marker, mineru
- 结果：✅ 通过

### 总结
- 通过用例数：6 / 6
- 失败原因分析：无
- 修复方向：无

### 备注
- 本机已安装 torch 但 CUDA .so 文件缺失（Windows 平台），导致 @patch("torch.cuda.xxx") 在 __enter__ 时触发 import 失败
- 修复方式：在 test_config.py 模块级别用 MagicMock 注入 sys.modules["torch"]，阻止真实的 torch import


## 第 2 次尝试（代码审查修复）

**时间**：2026-05-21 23:45:00
**状态**：✅ 通过

### 审查问题修复

#### C1: 添加 deploy.yaml M1 配置解析
- 新增 `M1Config` dataclass（backend_override, ocr_override, output_dir, quality_threshold）
- 新增 `load_m1_config(path)` 函数（yaml.safe_load + FileNotFoundError 回退默认值）
- 结果：✅ 通过

#### C2: 添加 nvidia-smi 检测
- 新增 `_check_nvidia_smi()` 函数（subprocess.run nvidia-smi + 5s timeout）
- 集成到 `detect_hardware()` 的 ImportError 兜底分支（torch 未安装时仍能检测 GPU）
- 结果：✅ 通过

#### C3: 添加遗漏的测试用例
- 测试用例 7 `test_load_m1_config`：tmp_path YAML 解析验证
- 测试用例 8 `test_load_m1_config_file_not_found`：文件不存在时返回默认值
- 测试用例 9 `test_recommend_ocr_engine_raises`：mock 所有引擎不可用，验证 RuntimeError
- 结果：✅ 9/9 通过

### 总结
- 通过用例数：9 / 9
- 失败原因分析：无
- 修复方向：无
