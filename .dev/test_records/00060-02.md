# 00060-02 - 格式路由器 测试记录

## 第 1 次尝试

**时间**：2026-05-21 23:50:00
**状态**：✅ 通过

### 测试用例 1：PDF magic bytes 检测
- 输入：临时文件以 b"%PDF-1.4\n%some content" 开头，后缀 .pdf
- 期望输出：FileFormat.PDF
- 实际输出：FileFormat.PDF
- 结果：✅ 通过

### 测试用例 2：DOCX detection via ZIP magic + extension
- 输入：临时文件以 b"PK\x03\x04" 开头，后缀 .docx
- 期望输出：FileFormat.DOCX
- 实际输出：FileFormat.DOCX
- 结果：✅ 通过

### 测试用例 3：PNG image detection via magic bytes
- 输入：临时文件以 b"\x89PNG\r\n\x1a\n" 开头，后缀 .png
- 期望输出：FileFormat.IMAGE
- 实际输出：FileFormat.IMAGE
- 结果：✅ 通过

### 测试用例 4：Unsupported format rejection
- 输入：临时文件以 b"random binary garbage" 开头，后缀 .xyz
- 期望输出：抛出 ValueError 含 "Unsupported"
- 实际输出：抛出 ValueError 含 "Unsupported file extension: .xyz"
- 结果：✅ 通过

### 测试用例 5：PDF routing to 3 valid backends + invalid backend rejection
- 输入：
  - route_backend(FileFormat.PDF, "docling")
  - route_backend(FileFormat.PDF, "marker")
  - route_backend(FileFormat.PDF, "mineru")
  - route_backend(FileFormat.PDF, "unknown_engine")
- 期望输出：前三返回对应后端字符串，最后抛出 ValueError 含 "Unsupported backend"
- 实际输出：前三返回 "docling"/"marker"/"mineru"，最后抛出 ValueError 含 "Unsupported backend 'unknown_engine'"
- 结果：✅ 通过

### 测试用例 6：DOCX forced routing to docling
- 输入：
  - route_backend(FileFormat.DOCX, "docling") → "docling"
  - route_backend(FileFormat.DOCX, "marker") → "docling" (forced, ignores user pref)
- 期望输出：两次调用均返回 "docling"
- 实际输出：两次调用均返回 "docling"
- 结果：✅ 通过

### 总结
- 通过用例数：6 / 6
- 失败原因分析：无
- 修复方向：无

### 备注
- 6 个新测试，全部首次通过。无修复迭代。
- Full test suite: 12/12 passed (6 config + 6 router).
- Magic bytes 检测覆盖：PDF (%PDF-), PNG (\x89PNG), ZIP/DOCX (PK\x03\x04), JPEG (\xff\xd8\xff), TIFF (II*\x00 / MM\x00*), BMP (BM), HTML (<!DOCTYP / <html).
- ZIP 系格式（DOCX/XLSX/PPTX 共享 PK\x03\x04 magic）通过后缀名做二次判断。
- FileFormat 采用 str+Enum 混入设计，方便 f-string 和 JSON 序列化。
