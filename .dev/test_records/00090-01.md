# 00090-01 - 骨架 + 配置 + 等级系统 测试记录

## 第 1 次尝试

**时间**：2026-06-03
**状态**：✅ 通过

### 测试用例 1：默认配置值
- 输入：构造 `QAConfig()` 不带参数
- 期望输出：llm 为默认 LLMBackend（provider=ollama, model=DeepSeek-V3），db_path="./data/m5_qa.db"，system_prompt_id="system_en"，retrieval_score_threshold=0.5，max_self_rag_iterations=3
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 2：自定义 LLM 后端
- 输入：`QAConfig(llm=LLMBackend(provider="deepseek", model="deepseek-chat", api_key="sk-test-12345", base_url="https://api.deepseek.com/v1"))`
- 期望输出：config.llm 各字段与被传入值一致，非 LLM 字段保持默认
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 3：自定义 db_path 和阈值
- 输入：`QAConfig(db_path="/prod/data/m5_qa_prod.db", retrieval_score_threshold=0.7, max_self_rag_iterations=5)`
- 期望输出：db_path、retrieval_score_threshold、max_self_rag_iterations 为非默认值，llm 保持默认
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 4：Basic 等级属性
- 输入：`USER_TIERS["basic"]`
- 期望输出：level="basic", context_window=4000, default_mode="simple", daily_premium_limit=3
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 5：Pro 等级属性
- 输入：`USER_TIERS["pro"]`
- 期望输出：level="pro", context_window=8000, default_mode="pipeline", daily_premium_limit=10
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 6：Premium 配额消耗/耗尽
- 输入：创建 total=3 的 PremiumQuota，连续 consume() 4 次
- 期望输出：前 3 次返回 True，第 4 次返回 False，used 停留在 3
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 7：Enterprise 无限 Premium
- 输入：`USER_TIERS["enterprise"]`
- 期望输出：level="enterprise", context_window=16000, default_mode="self_rag", daily_premium_limit=-1
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 8：Basic 用户获取 simple 模式
- 输入：`ModeRouter().select_mode(user_id="basic_user", tier_level="basic")`
- 期望输出：("simple", UserTier(level="basic"))
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 9：Pro 用户获取 pipeline 模式
- 输入：`ModeRouter().select_mode(user_id="pro_user", tier_level="pro")`
- 期望输出：("pipeline", UserTier(level="pro"))
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 10：Premium 配额升级到 self_rag
- 输入：Basic 用户 + MockQuotaManager（有 3 次 Premium），调用 select_mode
- 期望输出：mode="self_rag"，配额 consumed=1
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 11：Premium 耗尽可能回默认模式
- 输入：Pro 用户 + MockQuotaManager（total=0 已耗尽），调用 select_mode
- 期望输出：mode="pipeline"（回落到默认）
- 实际输出：完全符合预期
- 结果：✅ 通过

### 测试用例 12：未知等级 fallback 到 basic
- 输入：`ModeRouter().select_mode(user_id="unknown_user", tier_level="super_vip")`
- 期望输出：("simple", UserTier(level="basic"))
- 实际输出：完全符合预期
- 结果：✅ 通过

### 总结
- 通过用例数：12 / 12
- 失败用例数：0
- 覆盖率：config.py / tier.py / router.py 全部路径已覆盖
- 备注：测试比 Task 定义多出 3 个用例（Enterprise tier、Premium exhausted、unknown tier fallback），属于合理的边界条件扩展
