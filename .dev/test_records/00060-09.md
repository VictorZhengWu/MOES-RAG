# 00060-09 — M1 Chunking 封装 测试记录

## 第 1 次尝试

**时间**：2026-05-22 07:50:00
**状态**：✅ 通过

### 测试用例清单

#### test_chunker.py (2 tests)

1. **test_create_chunker_returns_none_when_docling_missing** — 依赖缺失时返回 None
   - 输入：patch.dict 移除 docling.chunking，调用 create_chunker()
   - 期望输出：返回 None，不抛异常
   - 实际输出：返回 None，无异常
   - 结果：✅ 通过

2. **test_create_chunker_returns_hybrid_chunker_when_available** — docling 安装后返回可用对象
   - 输入：create_chunker() 默认参数（BAAI/bge-small-en-v1.5, max_tokens=512）
   - 期望输出：HybridChunker 实例，tokenizer.max_tokens=512, merge_peers=True, repeat_table_header=True
   - 实际输出：HybridChunker 实例，所有配置项正确
   - 结果：✅ 通过

### 关键技术决策

- **API 升级**：从已弃用的字符串 tokenizer 参数升级到 HuggingFaceTokenizer 包装器对象（docling v2.95+ API）。`max_tokens` 从 HybridChunker 参数迁移到 HuggingFaceTokenizer 参数。
- **导入路径**：`HuggingFaceTokenizer` 需从 `docling_core.transforms.chunker.tokenizer.huggingface` 导入（`__init__.py` 未重新导出）。
- **优雅降级**：transformers 可用性检查 + tokenizer 加载异常捕获，双重保护避免运行时崩溃。

### 实现文件

| 文件 | 内容 |
|------|------|
| `m1_parser/output/chunker.py` | create_chunker() 工厂函数，封装 HybridChunker + HuggingFaceTokenizer |
| `m1_parser/output/__init__.py` | 新增 create_chunker 导出 |

### 验证命令

```bash
python -m pytest tests/test_chunker.py -v
```

### 验证结果

```
tests/test_chunker.py::test_create_chunker_returns_none_when_docling_missing PASSED
tests/test_chunker.py::test_create_chunker_returns_hybrid_chunker_when_available PASSED
2 passed in 11.33s
```

无 deprecation warning。
