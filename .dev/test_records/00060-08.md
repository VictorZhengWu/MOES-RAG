# 00060-08 — M1 序列化器与图片管理器 测试记录

## 第 1 次尝试

**时间**：2026-05-21 23:50:00
**状态**：🔄 待验证（Bash 工具受限，无法在当前会话执行 pytest）

### 测试用例清单

#### test_serializer.py (5 tests)

1. **test_save_markdown_creates_file** — save_markdown() 创建 {output_dir}/{doc_id}/full.md
   - 输入：content="# Test...", output_dir=tmp_path, doc_id="test_doc_001"
   - 期望输出：文件存在，UTF-8 编码，内容一致，CJK 字符保留
   - 实际输出：待运行
   - 结果：待验证

2. **test_save_json_creates_file** — save_json() 创建 {output_dir}/{doc_id}/full.json
   - 输入：data 含 CJK 字段的 dict, output_dir=tmp_path, doc_id="test_doc_002"
   - 期望输出：valid JSON, indent=2, ensure_ascii=False, CJK 原样保留
   - 实际输出：待运行
   - 结果：待验证

3. **test_custom_json_encoder_special_types** — CustomJSONEncoder 处理 datetime/Path/Enum/bytes
   - 输入：含 datetime, Path, Enum(str), bytes 的 dict
   - 期望输出：ISO 8601 string, POSIX path, enum value, base64 string
   - 实际输出：待运行
   - 结果：待验证

4. **test_save_markdown_rejects_invalid_inputs** — 输入校验
   - 输入：空 doc_id, whitespace doc_id, None content, 空 doc_id for save_json
   - 期望输出：全部抛出 ValueError
   - 实际输出：待运行
   - 结果：待验证

5. **test_save_markdown_handles_empty_content** — 空内容处理
   - 输入：content="", output_dir=tmp_path, doc_id="empty_doc"
   - 期望输出：文件创建成功，内容为空字符串
   - 实际输出：待运行
   - 结果：待验证

#### test_image_manager.py (7 tests)

6. **test_get_output_paths_creates_directories** — 创建 pages/figures/tables/ 目录
   - 输入：output_dir=tmp_path, doc_id="test_doc_001"
   - 期望输出：4 个目录全部存在，返回 dict 包含正确路径
   - 实际输出：待运行
   - 结果：待验证

7. **test_get_output_paths_idempotent** — 幂等性验证
   - 输入：同一 doc_id 调用两次
   - 期望输出：两次返回相同 dict
   - 实际输出：待运行
   - 结果：待验证

8. **test_get_output_paths_rejects_path_traversal** — 路径穿越拒绝
   - 输入：doc_id="../etc/cron", doc_id="sub/dir/doc", doc_id="sub\\dir\\doc", doc_id="/etc/passwd"
   - 期望输出：全部抛出 ValueError
   - 实际输出：待运行
   - 结果：待验证

9. **test_get_output_paths_rejects_empty_doc_id** — 空 doc_id 拒绝
   - 输入：doc_id="", doc_id="   "
   - 期望输出：抛出 ValueError
   - 实际输出：待运行
   - 结果：待验证

10. **test_save_figure_metadata_writes_sidecar** — 写入 .meta.json 侧车
    - 输入：figure_path+metadata (含 CJK 字段)
    - 期望输出：sidecar 存在，JSON 有效，CJK 原样保留
    - 实际输出：待运行
    - 结果：待验证

11. **test_save_figure_metadata_creates_parent_dirs** — 自动创建父目录
    - 输入：不存在的 figure_path
    - 期望输出：父目录和 sidecar 均创建成功
    - 实际输出：待运行
    - 结果：待验证

12. **test_save_figure_metadata_validates_inputs** — 输入校验
    - 输入：空 metadata dict, 空 figure_path, None figure_path
    - 期望输出：全部抛出 ValueError
    - 实际输出：待运行
    - 结果：待验证

### 实现文件

| 文件 | 内容 |
|------|------|
| `m1_parser/output/__init__.py` | 导出 save_markdown, save_json, CustomJSONEncoder, get_output_paths, save_figure_metadata |
| `m1_parser/output/serializer.py` | save_markdown + save_json + CustomJSONEncoder + _validate_inputs |
| `m1_parser/output/image_manager.py` | get_output_paths + save_figure_metadata + _validate_doc_id |

### 验证命令

```bash
python -m pytest tests/test_serializer.py tests/test_image_manager.py -v
```
