# 00060-12 - 打包与最终验证 测试记录

## 第 1 次尝试

**时间**：2026-05-21 23:00
**状态**：✅ 通过

### 验证步骤

#### 1. 全量测试套件
- 命令：`python -m pytest m1-doc-parsing/tests/ -v`
- 结果：118 passed, 0 failed, 5 warnings
- 修复：修复了 `test_create_chunker_returns_hybrid_chunker_when_available` 的测试隔离问题（conftest 未清理 fake torch MagicMock）；修复了 chunker.py 中 transformers 导入的 ValueError 捕获（`except ImportError` → `except Exception`）
- ✅ 通过

#### 2. pip install 验证
- 命令：`pip install -e m1-doc-parsing/`
- 结果：Successfully installed m1-doc-parsing-0.1.0
- ✅ 通过

#### 3. 公开 API 导入验证
- 命令：`python -c "from m1_parser import convert, detect_hardware; print('OK')"`
- 结果：OK
- ✅ 通过

#### 4. CLI 可执行验证
- 命令：`python -m m1_parser.standalone.cli --help`
- 结果：显示使用说明，包含 convert 子命令和所有选项
- ✅ 通过

### 修复说明
- **chunker.py**: `except ImportError` → `except Exception`（第 126 行），防止 `transformers` 模块级代码的 `ValueError: torch.__spec__ is not set` 导致崩溃
- **test_chunker.py**: 移除 `sys.modules.pop` 模式，改用简单的 `patch.dict` 避免模块缓存污染
- **conftest.py**: 将 `test_chunker` 加入 fake torch 清理列表，与 `test_docling_backend`/`test_converter` 保持一致

### 总结
- 通过：4 / 4 项验证全部通过
- 全量测试：118 / 118 通过
- 公开 API：`convert`, `detect_hardware` 可正确导入
- CLI：`--help` 正常显示
- pip install 可成功
