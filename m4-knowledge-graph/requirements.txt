# M4 Knowledge Graph Engine — Python Dependencies
#
# Core dependencies (also listed in pyproject.toml):
kuzu>=0.3.0
openai>=1.0.0
httpx>=0.27.0

# Development dependencies:
pytest>=8.0
pytest-asyncio>=0.23.0
